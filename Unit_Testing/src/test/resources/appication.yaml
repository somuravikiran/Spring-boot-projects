spring:
  datasource:
    url: jdbc:h2://mem:db;DB_close_DELAY=-1
    username: root
    password: W7301@jqir#
    driver-class-name: org.h2.driver

  jpa:
    properties:
      hibernate:
        dialect: org.hiebernate.dialect.H2Dialect
    show-sql: true

  jpa.hibernate.ddl-auto: create-drop
